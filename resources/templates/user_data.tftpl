#!/bin/bash
export DEBIAN_FRONTEND=noninteractive
apt-get update && apt-get install -y python3

# Inject the Rendered Python Script
cat <<'EOF' > /usr/local/bin/hydra_init.py
${python_init_content}
EOF

# Run Orchestrator
python3 /usr/local/bin/hydra_init.py